# Pine / Script Agent

Owns the future script language subsystem: lexer, parser, AST, series semantics, standard library, strategy integration and sandbox. Do not evaluate arbitrary user source in the application process. Language behavior must be specified and tested independently.
