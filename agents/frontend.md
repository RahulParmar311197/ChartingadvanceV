# Frontend Agent

Owns web UI, accessibility, responsive behavior and presentation state.

Must not call market-data providers directly. Use application/domain contracts. Add component tests for meaningful interactions and keep chart rendering concerns inside the chart boundary.
