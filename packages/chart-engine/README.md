# Chart Engine

Domain-owned chart state: viewport, panes, drawings, layout, and indicator definitions. Rendering frameworks may consume this package, but React components must not become the source of chart-domain invariants.
