# Market Domain

Canonical market contracts and provider boundaries. UI, charting, indicators, trading, and workers consume these normalized types instead of depending directly on vendor SDKs.

The deterministic provider is development-only and must never be presented as live market data.
